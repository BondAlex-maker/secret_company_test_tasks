cd /var/www/html перейти в папку html
ls -a вывести список всех файлов в папке html
cd ../../logs выйти из папки html и www, зайти в папку logs
grep 404 access.log найти все упоминания строки 404
cd ../www/html выйти из папки logs и войти в узазанные папки в пути.
mv 404.html 403.html изменить название файла 404 на 403
sed -i 's/404/403/g' 403.html sed - (Stream EDitor -i на месте (т.е. сохранить обратно в исходный файл) s = команда замены
404 слово для замены
403 = текст на который заменяеться
g глобальный флаг для прохода и изменений по всему файлу , а не только первого совпадения в файле access.logs)

sudo chmod ugo-wrx ./index.html меняем с помощью прав суперпользователя убираем права на чтение запись и выполнение index.html всем (пользователю, группе, остальным).
chmod u+rw index.html пользователю даем права на чтение и запись в файл
chmod g+r index.html группе даем права на чтение файла

ln -s ../../logs/access.log softlink указываем путь к файлу аксесс.лог выйдя из html и www создаем софтлинк в папке html

cd /var заходим в папку var
find * -maxdepth 3 -type f найти все файлы до третьего уровня сложенности

touch 503.html создаем файл 503.html в папке хтмл
nano 503.html открываем файл с помощью редактора нано
<html>
    <head>
        <title>503</title>
    </head>
</html> копипастим хед страницы
сохраняем ctrl+x > y > enter
